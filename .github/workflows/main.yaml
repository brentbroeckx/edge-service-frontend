name: Build, and deliver frontend app container

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    name: Java 11 build, test, deliver container
    steps:
      - name: Check out repository
        uses: actions/checkout@v1

      - name: Set up JDK 11
        uses: actions/setup-java@v1
        with:
          java-version: 11

#      - name: Install Google Chrome 96.0.4664.45
#        run: host https://github.com/brentbroeckx/edge-service-frontend/tree/main/chromedownloads/google-chrome-stable_96.0.4664.45-1_amd64.deb \
#          && sudo dpkg -y /tmp/chrome.deb \
#          && rm /tmp/chrome.deb

      - name: Chromeversion check
        run: google-chrome --version

#      - name: Install ChromeDriver
#        run: |
#            CHROME_VERSION=$(google-chrome --version | cut -f 3 -d ' ' | cut -d '.' -f 1) \
#            && CHROMEDRIVER_RELEASE=$(curl --location --fail --retry 3 http://chromedriver.storage.googleapis.com/LATEST_RELEASE_${CHROME_VERSION}) \
#            && curl --silent --show-error --location --fail --retry 3 --output /tmp/chromedriver_linux64.zip "http://chromedriver.storage.googleapis.com/$CHROMEDRIVER_RELEASE/chromedriver_linux64.zip" \
#            && cd /tmp \
#            && unzip chromedriver_linux64.zip \
#            && rm -rf chromedriver_linux64.zip \
#            && sudo mv chromedriver /usr/local/bin/chromedriver \
#            && sudo chmod +x /usr/local/bin/chromedriver \
#            && chromedriver --version

      - name: Build
        run: mvn test --file pom.xml

      - name: Docker login
        run: docker login -u ${{ secrets.DOCKER_USER }} -p ${{ secrets.DOCKER_PASSWORD }}

      - name: Docker Build
        run: docker build -t ${{ secrets.DOCKER_USER }}/edge-service-frontend:latest .

      - name: Upload container to Docker Hub with Push
        run: docker push ${{ secrets.DOCKER_USER }}/edge-service-frontend:latest